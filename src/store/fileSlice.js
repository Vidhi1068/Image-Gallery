// import { createSlice } from "@reduxjs/toolkit";

// const fileSlice = createSlice({
//   name: file,

//   initialState: [],

//   reducers: {
//     upload(state, action) {
//       state.push(action.payload);
//     },

//     matchImg(state, action) {
//       return state.filter((img) => img.name !== action.payload);
//     },
//   },
// });

// export const { upload, matchImg } = fileSlice.actions;
// export default fileSlice.reducer;
